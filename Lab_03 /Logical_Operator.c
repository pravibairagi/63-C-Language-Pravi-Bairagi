#include<stdio.h>
int main ()
{
    int a = 12 ;
    int b = 23 ;
    printf("Logical AND (a && b ) : %d \n", (a && b));
    printf("Logical OR (a || b ) : %d \n", (a|| b));
    printf("Logical NOT (! b)   : %d \n", (! b)) ; 
    return 0 ;

} 
