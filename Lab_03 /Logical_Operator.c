#include<stdio.h>
int main ()
{
    int a = 12 ;
    int b = 23 ;
    printf("(a && b ) : %d (Logical AND)\n", (a && b));
    printf("(a || b ) : %d (Logical OR)\n", (a|| b));
    printf("(! b)   : %d (Logical NOT)\n", (! b)) ; 
    return 0 ;

} 
